name: ci

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java-version: ["17", "21", "25", "26", "27"]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK
        run: docker --version

      - name: Verify
        run: make verify JAVA_VERSION=${{ matrix.java-version }}

      - name: App-driven smoke
        run: make smoke JAVA_VERSION=${{ matrix.java-version }}

  spring-boot-current:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify current Spring Boot on current Java
        run: make verify JAVA_VERSION=27 SPRING_BOOT_VERSION=4.1.1

  wildfly:
    name: WildFly Jakarta and Javax multi-WAR smoke
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run app-server smoke
        run: make smoke-wildfly JAVA_VERSION=21
